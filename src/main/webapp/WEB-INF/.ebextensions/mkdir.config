commands:
  create_indexes_dir:
    command: "mkdir -p /usr/lucene/indexes"
    ignoreErrors: true
  permissions_indexes_dir:
    command: "chmod 777 /usr/lucene/indexes"
  owner_indexes_dir:
    command: "chown -R tomcat /usr/lucene"